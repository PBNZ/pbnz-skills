# macOS
.DS_Store
.AppleDouble
.LSOverride

# Windows
Thumbs.db
ehthumbs.db
Desktop.ini
$RECYCLE.BIN/

# Editors / IDEs
.idea/
.vscode/
*.swp
*.swo
*~

# Logs and temp files
*.log
*.tmp
.cache/

# Node (in case local tooling is added later)
node_modules/
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# Python (in case local tooling is added later)
__pycache__/
*.py[cod]
.venv/
venv/
.env
.env.local

# Build artifacts
dist/
build/

# Overrides — repo-tracked files that a contributor's *global* gitignore might
# otherwise match (e.g. a common `*private*` rule). Keep these negations so
# these files can never be silently dropped from a commit.
!scripts/check_no_private_contact.py
